<form class="{{cssClass}}" autocomplete="off">
  {{!-- Sheet Header --}}
  <header class="sheet-header">
    <img
      class="item-img"
      src="{{item.img}}"
      data-edit="img"
      title="{{item.name}}"
    />
    <div class="header-fields">
      <h1 class="charname">
        <input
          name="name"
          type="text"
          value="{{item.name}}"
          placeholder="{{localize "hack100.placeholders.itemName"}}"
        />
      </h1>
    </div>
  </header>

  {{!-- Sheet Tab Navigation --}}
  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-tab="description">{{localize "hack100.items.description"}}</a>
    <a class="item" data-tab="attributes">{{localize "hack100.items.attributes"}}</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class="sheet-body">

    {{!-- Description Tab --}}
    <div class="tab" data-group="primary" data-tab="description">
      <div class="item-description-section">
        <div class="form-group">
          <label>{{localize "hack100.items.description"}}</label>
          <textarea
            name="system.description"
            rows="10"
            placeholder="{{localize "hack100.placeholders.descriptionItem"}}"
          >{{system.description}}</textarea>
        </div>
      </div>
    </div>

    {{!-- Attributes Tab --}}
    <div class="tab" data-group="primary" data-tab="attributes">

      {{!-- Location --}}
      <div class="form-group">
        <label>{{localize "hack100.items.location"}}</label>
        <input
          type="text"
          name="system.location"
          value="{{system.location}}"
          placeholder="{{localize "hack100.placeholders.itemLocation"}}"
        />
        <p class="hint">{{localize "hack100.hints.itemLocation"}}</p>
      </div>

      {{!-- Quantity, Weight, Price Row --}}
      <div class="item-details-row">
        <div class="form-group">
          <label>{{localize "hack100.items.quantity"}}</label>
          <input
            type="number"
            name="system.quantity"
            value="{{system.quantity}}"
            min="0"
            data-dtype="Number"
          />
        </div>

        <div class="form-group">
          <label>{{localize "hack100.items.weightPerItem"}}</label>
          <input
            type="number"
            name="system.weight"
            value="{{system.weight}}"
            step="0.1"
            data-dtype="Number"
            placeholder="{{localize "hack100.placeholders.weight"}}"
          />
        </div>

        <div class="form-group">
          <label>{{localize "hack100.items.price"}}</label>
          <input
            type="number"
            name="system.price"
            value="{{system.price}}"
            data-dtype="Number"
            placeholder="{{localize "hack100.placeholders.price"}}"
          />
        </div>
      </div>

      {{!-- Equipped Checkbox --}}
      <div class="form-group checkbox-group">
        <label class="checkbox-label">
          <input
            type="checkbox"
            name="system.equipped"
            {{checked system.equipped}}
          />
          <span class="checkbox-text">{{localize "hack100.items.equippedInUse"}}</span>
        </label>
        <p class="hint">{{localize "hack100.hints.itemEquipped"}}</p>
      </div>

    </div>
  </section>
</form>
